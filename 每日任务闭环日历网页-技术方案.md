# 每日任务闭环日历网页技术方案

## 1. 推荐技术栈

## 推荐选择：React + TypeScript + Vite

在 React、Vue、Next.js 三个选项中，推荐使用：

```text
React + TypeScript + Vite
```

### 选择理由

#### 1. MVP 开发速度快

本项目第一阶段是单页面网页应用，数据保存在浏览器 localStorage，不需要服务端渲染、不需要后端接口、不需要登录系统。

React + Vite 可以快速启动项目，适合先验证核心产品体验。

---

#### 2. 组件化适合日历和任务面板

本项目页面天然适合组件拆分：

- Calendar
- CalendarDay
- TaskPanel
- TaskItem
- TaskForm
- ReviewBox
- StatsBar

React 的组件模型非常适合这种交互型页面。

---

#### 3. TypeScript 有利于数据模型稳定

任务闭环涉及多种状态：

- 未开始
- 进行中
- 已完成
- 已延期
- 已取消

使用 TypeScript 可以提前约束任务状态、优先级、复盘数据结构，减少后续扩展时的维护成本。

---

#### 4. 后续可平滑扩展云端同步

MVP 阶段使用 localStorage。

后续如果需要登录账号和云端同步，可以将数据层从 localStorage 替换为 API 调用，而组件层基本不需要大改。

推荐提前抽象 storage 层：

```text
页面组件 → hooks → storage service → localStorage
```

未来可替换为：

```text
页面组件 → hooks → API service → 云端数据库
```

---

### 为什么不优先选 Vue

Vue 也可以很好完成该项目，但如果后续要引入更复杂的状态组合、AI 助手、多人协作和数据同步，React 生态中的状态管理、组件库和工程实践选择更多。

---

### 为什么不优先选 Next.js

Next.js 更适合需要服务端渲染、路由体系、登录鉴权、服务端 API 的项目。

当前 MVP 是本地单页面应用，使用 Next.js 会增加不必要的复杂度。

等后续进入账号系统和云端同步阶段，可以再评估是否迁移到 Next.js。

---

## 2. 项目结构设计

推荐目录结构如下：

```text
E:\刘源\daima\vscode
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── src
│   ├── main.tsx
│   ├── App.tsx
│   ├── components
│   │   ├── Calendar
│   │   │   ├── Calendar.tsx
│   │   │   ├── CalendarDay.tsx
│   │   │   └── calendar.css
│   │   ├── TaskPanel
│   │   │   ├── TaskPanel.tsx
│   │   │   ├── TaskItem.tsx
│   │   │   ├── TaskForm.tsx
│   │   │   └── task-panel.css
│   │   ├── ReviewBox
│   │   │   ├── ReviewBox.tsx
│   │   │   └── review-box.css
│   │   └── StatsBar
│   │       ├── StatsBar.tsx
│   │       └── stats-bar.css
│   ├── hooks
│   │   ├── useCalendar.ts
│   │   ├── useTasks.ts
│   │   └── useLocalStorage.ts
│   ├── services
│   │   └── storage.ts
│   ├── types
│   │   └── task.ts
│   ├── utils
│   │   ├── date.ts
│   │   ├── stats.ts
│   │   └── task.ts
│   └── styles
│       ├── reset.css
│       ├── variables.css
│       └── global.css
└── README.md
```

---

### 目录职责说明

| 目录 | 职责 |
|---|---|
| src/components | 页面 UI 组件 |
| src/hooks | 封装状态逻辑和业务逻辑 |
| src/services | 数据读写层，MVP 先封装 localStorage |
| src/types | TypeScript 类型定义 |
| src/utils | 日期计算、统计计算、任务处理函数 |
| src/styles | 全局样式、变量、重置样式 |

---

### 结构设计原则

#### 1. UI 和数据存储解耦

组件不直接操作 localStorage。

不推荐：

```text
TaskItem → localStorage.setItem
```

推荐：

```text
TaskItem → useTasks → storage service → localStorage
```

这样未来替换云端 API 更容易。

---

#### 2. 统计逻辑放 utils

完成率、任务数量、延期数量等逻辑不应写死在组件里。

推荐放在：

```text
src/utils/stats.ts
```

例如：

```ts
calculateDayStats(tasks, date)
```

---

#### 3. 日期逻辑单独封装

日历页面会大量处理日期：

- 当前月份
- 月份切换
- 获取某月所有日期
- 判断今天
- 判断选中日期
- 获取明天日期

推荐放在：

```text
src/utils/date.ts
```

---

## 3. 数据模型设计

## TaskStatus

任务状态。

```ts
export type TaskStatus =
  | 'todo'
  | 'in_progress'
  | 'completed'
  | 'postponed'
  | 'cancelled';
```

### 字段说明

| 值 | 含义 |
|---|---|
| todo | 未开始 |
| in_progress | 进行中 |
| completed | 已完成 |
| postponed | 已延期 |
| cancelled | 已取消 |

MVP 先不加入 `split` 和 `failed`，避免第一版复杂度过高。

---

## TaskPriority

任务优先级。

```ts
export type TaskPriority = 'low' | 'medium' | 'high';
```

### 字段说明

| 值 | 含义 |
|---|---|
| low | 低优先级 |
| medium | 中优先级 |
| high | 高优先级 |

---

## Task

任务实体。

```ts
export interface Task {
  id: string;
  title: string;
  date: string;
  status: TaskStatus;
  priority: TaskPriority;
  postponedFrom?: string;
  createdAt: string;
  updatedAt: string;
}
```

### 字段说明

| 字段 | 类型 | 用途 |
|---|---|---|
| id | string | 任务唯一 ID |
| title | string | 任务标题 |
| date | string | 任务所属日期，格式 YYYY-MM-DD |
| status | TaskStatus | 当前任务状态 |
| priority | TaskPriority | 任务优先级 |
| postponedFrom | string | 如果任务从其他日期延期而来，记录来源日期 |
| createdAt | string | 创建时间，ISO 字符串 |
| updatedAt | string | 更新时间，ISO 字符串 |

---

## DayReview

每日复盘记录。

```ts
export interface DayReview {
  date: string;
  planConfirmed: boolean;
  closed: boolean;
  completedSummary: string;
  unfinishedReason: string;
  tomorrowImprovement: string;
  score?: number;
  createdAt: string;
  updatedAt: string;
}
```

### 字段说明

| 字段 | 类型 | 用途 |
|---|---|---|
| date | string | 复盘所属日期，格式 YYYY-MM-DD |
| planConfirmed | boolean | 是否已确认今日计划 |
| closed | boolean | 当天是否已完成闭环 |
| completedSummary | string | 今日完成了什么 |
| unfinishedReason | string | 未完成原因 |
| tomorrowImprovement | string | 明天改进点 |
| score | number | 今日评分，1-5，可选 |
| createdAt | string | 创建时间，ISO 字符串 |
| updatedAt | string | 更新时间，ISO 字符串 |

---

## CalendarDayStats

日历单元格统计数据。

```ts
export interface CalendarDayStats {
  date: string;
  totalTasks: number;
  completedTasks: number;
  postponedTasks: number;
  cancelledTasks: number;
  unfinishedTasks: number;
  completionRate: number;
  hasPostponedTasks: boolean;
  isPlanConfirmed: boolean;
  isClosed: boolean;
}
```

### 字段说明

| 字段 | 类型 | 用途 |
|---|---|---|
| date | string | 日期，格式 YYYY-MM-DD |
| totalTasks | number | 当天任务总数 |
| completedTasks | number | 已完成任务数 |
| postponedTasks | number | 已延期任务数 |
| cancelledTasks | number | 已取消任务数 |
| unfinishedTasks | number | 未完成任务数，包括 todo 和 in_progress |
| completionRate | number | 完成率，范围 0-100 |
| hasPostponedTasks | boolean | 是否存在延期任务 |
| isPlanConfirmed | boolean | 当天是否已确认计划 |
| isClosed | boolean | 当天是否已完成闭环 |

---

## LocalStorageData

为了支持数据版本升级，建议 localStorage 中保存一个整体对象。

```ts
export interface LocalStorageData {
  version: number;
  tasks: Task[];
  reviews: DayReview[];
}
```

### 字段说明

| 字段 | 类型 | 用途 |
|---|---|---|
| version | number | 数据结构版本号，方便后续升级 |
| tasks | Task[] | 所有任务列表 |
| reviews | DayReview[] | 所有每日复盘记录 |

---

## 4. 状态管理方案

## 推荐方案：React State + 自定义 Hooks + localStorage

MVP 阶段不建议引入 Redux、MobX、Zustand 等状态管理库。

原因：

1. 当前状态规模不大
2. 单页面应用，不涉及复杂跨页面状态
3. 自定义 hooks 足够支撑 MVP
4. 减少依赖和项目复杂度

---

## 哪些状态放 React state

以下状态属于 UI 运行时状态，放在 React state 中：

```ts
const [currentMonth, setCurrentMonth] = useState<string>();
const [selectedDate, setSelectedDate] = useState<string>();
const [taskDraft, setTaskDraft] = useState<string>();
const [editingTaskId, setEditingTaskId] = useState<string | null>();
```

### UI 状态清单

| 状态 | 用途 | 是否持久化 |
|---|---|---|
| currentMonth | 当前查看的月份 | 可选，不必须 |
| selectedDate | 当前选中的日期 | 可选，不必须 |
| taskDraft | 添加任务输入框内容 | 不持久化 |
| editingTaskId | 当前正在编辑的任务 ID | 不持久化 |
| modalState | 弹窗状态 | 不持久化 |

---

## 哪些数据持久化到 localStorage

以下属于业务数据，需要持久化：

| 数据 | 是否持久化 | 原因 |
|---|---|---|
| tasks | 是 | 页面刷新后任务不能丢失 |
| reviews | 是 | 每日复盘需要长期保存 |
| planConfirmed | 是 | 日期闭环状态的一部分 |
| closed | 是 | 日期闭环状态的一部分 |

---

## 页面刷新后如何恢复数据

页面加载时执行：

```text
读取 localStorage
  ↓
检查数据是否存在
  ↓
如果不存在，使用默认空数据
  ↓
如果存在，检查 version
  ↓
必要时执行数据升级
  ↓
写入 React state
```

推荐封装为：

```ts
const { tasks, reviews, addTask, updateTask, deleteTask } = useTasks();
```

组件不需要关心 localStorage 细节。

---

## 5. localStorage 存储设计

## key 命名

推荐使用统一命名空间，避免和其他应用冲突。

```ts
const STORAGE_KEY = 'daily-task-closure-calendar:data';
```

如果后续拆分多个 key，可以使用：

```ts
const TASKS_KEY = 'daily-task-closure-calendar:tasks';
const REVIEWS_KEY = 'daily-task-closure-calendar:reviews';
const META_KEY = 'daily-task-closure-calendar:meta';
```

MVP 推荐一个整体 key，便于读写和导出。

---

## 存储格式

```json
{
  "version": 1,
  "tasks": [
    {
      "id": "task_001",
      "title": "完成产品方案",
      "date": "2026-05-03",
      "status": "in_progress",
      "priority": "high",
      "createdAt": "2026-05-03T08:00:00.000Z",
      "updatedAt": "2026-05-03T10:00:00.000Z"
    }
  ],
  "reviews": [
    {
      "date": "2026-05-03",
      "planConfirmed": true,
      "closed": false,
      "completedSummary": "完成了主要工作任务",
      "unfinishedReason": "临时事项较多",
      "tomorrowImprovement": "明天减少任务数量",
      "score": 4,
      "createdAt": "2026-05-03T20:00:00.000Z",
      "updatedAt": "2026-05-03T20:30:00.000Z"
    }
  ]
}
```

---

## 读写方法设计

建议在 `src/services/storage.ts` 中封装。

```ts
const STORAGE_KEY = 'daily-task-closure-calendar:data';
const CURRENT_VERSION = 1;

export function loadData(): LocalStorageData {
  const raw = localStorage.getItem(STORAGE_KEY);

  if (!raw) {
    return createEmptyData();
  }

  try {
    const parsed = JSON.parse(raw) as LocalStorageData;
    return migrateData(parsed);
  } catch {
    return createEmptyData();
  }
}

export function saveData(data: LocalStorageData): void {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(data));
}

function createEmptyData(): LocalStorageData {
  return {
    version: CURRENT_VERSION,
    tasks: [],
    reviews: []
  };
}
```

---

## 数据升级预留方式

使用 `version` 字段做数据迁移。

```ts
function migrateData(data: LocalStorageData): LocalStorageData {
  if (!data.version) {
    return migrateFromLegacy(data);
  }

  if (data.version === 1) {
    return data;
  }

  return data;
}
```

后续如果新增字段，例如任务标签 `tags`，可以写：

```ts
function migrateV1ToV2(data: LocalStorageData): LocalStorageDataV2 {
  return {
    version: 2,
    tasks: data.tasks.map(task => ({
      ...task,
      tags: []
    })),
    reviews: data.reviews
  };
}
```

---

## 6. 组件拆分方案

## App

### 职责

应用根组件，负责整体布局和核心状态组合。

### 管理内容

- 当前月份
- 当前选中日期
- 任务数据
- 复盘数据
- 页面布局

### 子组件

```text
App
├── Calendar
├── TaskPanel
└── StatsBar
```

---

## Calendar

### 职责

展示月历视图。

### Props 草案

```ts
interface CalendarProps {
  currentMonth: string;
  selectedDate: string;
  tasks: Task[];
  reviews: DayReview[];
  onSelectDate: (date: string) => void;
  onChangeMonth: (month: string) => void;
}
```

### 主要功能

- 渲染当前月份日期
- 切换上个月 / 下个月
- 点击日期选择
- 给每个日期计算统计数据

---

## CalendarDay

### 职责

渲染单个日期格子。

### Props 草案

```ts
interface CalendarDayProps {
  date: string;
  stats: CalendarDayStats;
  isToday: boolean;
  isSelected: boolean;
  onClick: (date: string) => void;
}
```

### 主要功能

- 展示日期数字
- 展示任务完成数
- 展示完成率
- 展示延期标识
- 展示今天和选中状态

---

## TaskPanel

### 职责

展示和管理选中日期的任务、完成率和复盘。

### Props 草案

```ts
interface TaskPanelProps {
  date: string;
  tasks: Task[];
  review: DayReview | null;
  stats: CalendarDayStats;
  onAddTask: (title: string, priority: TaskPriority) => void;
  onUpdateTask: (taskId: string, patch: Partial<Task>) => void;
  onDeleteTask: (taskId: string) => void;
  onPostponeTask: (taskId: string) => void;
  onUpdateReview: (patch: Partial<DayReview>) => void;
  onCloseDay: () => void;
}
```

### 子组件

```text
TaskPanel
├── StatsBar
├── TaskForm
├── TaskItem
└── ReviewBox
```

---

## TaskItem

### 职责

展示单条任务并提供任务操作。

### Props 草案

```ts
interface TaskItemProps {
  task: Task;
  onComplete: (taskId: string) => void;
  onEdit: (taskId: string, patch: Partial<Task>) => void;
  onDelete: (taskId: string) => void;
  onPostpone: (taskId: string) => void;
}
```

### 主要功能

- 展示任务标题
- 展示优先级
- 展示状态
- 完成任务
- 编辑任务
- 删除任务
- 延期到明天

---

## TaskForm

### 职责

快速添加任务。

### Props 草案

```ts
interface TaskFormProps {
  onSubmit: (title: string, priority: TaskPriority) => void;
}
```

### 主要功能

- 输入任务标题
- 选择优先级
- 点击添加
- 回车添加
- 提交后清空输入框

---

## ReviewBox

### 职责

每日复盘输入区域。

### Props 草案

```ts
interface ReviewBoxProps {
  review: DayReview | null;
  onChange: (patch: Partial<DayReview>) => void;
}
```

### 主要功能

- 编辑今日完成内容
- 编辑未完成原因
- 编辑明天改进点
- 编辑评分
- 自动保存

---

## StatsBar

### 职责

展示当天完成情况。

### Props 草案

```ts
interface StatsBarProps {
  stats: CalendarDayStats;
}
```

### 展示内容

- 完成率
- 已完成 / 总任务
- 未完成数量
- 延期数量
- 闭环状态

---

## 7. 核心业务逻辑设计

## 添加任务

```text
用户输入任务标题
  ↓
创建 Task 对象
  ↓
加入 tasks
  ↓
保存到 localStorage
  ↓
刷新日历统计和任务面板
```

---

## 完成任务

```text
点击完成按钮
  ↓
任务 status 改为 completed
  ↓
更新 updatedAt
  ↓
保存到 localStorage
  ↓
重新计算完成率
```

---

## 延期任务

```text
点击延期到明天
  ↓
当前任务 status 改为 postponed
  ↓
创建一条明天的新任务
  ↓
新任务 postponedFrom = 当前日期
  ↓
保存到 localStorage
```

说明：

MVP 推荐采用“保留原任务记录 + 创建新任务”的方式。

好处：

- 当天可以保留延期记录
- 明天可以继续执行任务
- 后续方便统计任务延期次数

---

## 完成今日闭环

点击「完成今日闭环」时检查：

```text
1. 今日计划是否已确认
2. 是否还有 todo 或 in_progress 任务
3. 是否填写复盘内容
```

如果检查通过：

```text
review.closed = true
```

如果检查不通过：

提示用户先处理未完成任务。

---

## 8. MVP 推荐开发步骤

## 第一步：初始化项目

目标：建立 React + TypeScript + Vite 项目基础。

产出：

- package.json
- vite.config.ts
- tsconfig.json
- src/main.tsx
- src/App.tsx
- 全局样式文件

---

## 第二步：定义类型和数据模型

目标：先稳定核心数据结构。

产出：

- TaskStatus
- TaskPriority
- Task
- DayReview
- CalendarDayStats
- LocalStorageData

文件：

```text
src/types/task.ts
```

---

## 第三步：实现 localStorage 数据层

目标：完成数据持久化基础能力。

产出：

- loadData
- saveData
- createEmptyData
- migrateData

文件：

```text
src/services/storage.ts
```

---

## 第四步：实现日期工具函数

目标：支撑月历渲染。

产出：

- 获取当前日期
- 格式化日期
- 获取某月日期列表
- 判断是否今天
- 获取明天日期
- 切换月份

文件：

```text
src/utils/date.ts
```

---

## 第五步：实现任务 hooks

目标：统一管理任务和复盘数据。

产出：

- useTasks
- addTask
- updateTask
- deleteTask
- postponeTask
- updateReview
- closeDay

文件：

```text
src/hooks/useTasks.ts
```

---

## 第六步：实现统计工具函数

目标：计算每天任务统计。

产出：

- calculateDayStats
- calculateCompletionRate
- getTasksByDate

文件：

```text
src/utils/stats.ts
```

---

## 第七步：实现月历组件

目标：完成页面核心视图。

产出：

- Calendar
- CalendarDay
- 月份切换
- 日期选择
- 日期状态展示

文件：

```text
src/components/Calendar
```

---

## 第八步：实现任务面板

目标：用户可以管理选中日期任务。

产出：

- TaskPanel
- TaskForm
- TaskItem
- 新增任务
- 完成任务
- 编辑任务
- 删除任务
- 延期到明天

文件：

```text
src/components/TaskPanel
```

---

## 第九步：实现完成率展示

目标：用户能快速理解当天执行情况。

产出：

- StatsBar
- 完成率进度条
- 已完成 / 总任务
- 未完成数
- 延期数

文件：

```text
src/components/StatsBar
```

---

## 第十步：实现每日复盘

目标：支持每日闭环中的复盘环节。

产出：

- ReviewBox
- 今日完成了什么
- 未完成原因
- 明天改进点
- 自动保存到 localStorage

文件：

```text
src/components/ReviewBox
```

---

## 第十一步：实现闭环校验

目标：完成任务闭环核心机制。

规则：

```text
如果存在 todo 或 in_progress 任务，不允许闭环
如果复盘内容为空，不允许闭环
满足条件后，将当天 closed 设置为 true
```

产出：

- 完成今日闭环按钮
- 禁用状态
- 错误提示
- 已闭环状态展示

---

## 第十二步：样式和响应式优化

目标：完成基础视觉体验。

产出：

- 桌面端双栏布局
- 日历单元格状态色
- 任务卡片样式
- 移动端上下布局
- 基础交互反馈

---

## 9. MVP 范围确认

## MVP 必做

第一版建议只实现：

1. 月历视图
2. 日期选择
3. 任务新增、编辑、删除
4. 任务状态切换
5. 延期到明天
6. 完成率展示
7. 每日复盘
8. 完成今日闭环
9. localStorage 保存和恢复

---

## MVP 暂不做

第一版暂不做：

- 登录注册
- 云端同步
- 多用户协作
- AI 任务拆解
- 任务拖拽
- 复杂统计图表
- 重复任务
- 标签系统
- 消息提醒

---

## 10. 后续扩展方向

## 云端同步扩展

未来可以将 storage 层替换为 API service。

MVP：

```text
useTasks → storage.ts → localStorage
```

云端版：

```text
useTasks → taskApi.ts → server database
```

---

## 登录账号扩展

增加用户系统后，数据模型可增加：

```ts
userId: string;
```

Task 和 DayReview 都可以关联 userId。

---

## 高级统计扩展

基于现有任务和复盘数据，可以计算：

- 连续闭环天数
- 每周完成率
- 每月完成率
- 延期次数最多的任务
- 高优先级任务完成率
- 常见未完成原因

---

# 最终建议

本项目第一版推荐使用 React + TypeScript + Vite，采用单页面架构，先用 localStorage 保存所有任务和复盘数据。

架构重点是提前抽象数据层和类型模型，让 MVP 能快速上线，同时为后续登录账号、云端同步、AI 分析和统计报表保留扩展空间。